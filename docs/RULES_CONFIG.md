# Rules Configuration

## Intent Keywords
Defined in `app/rules.py` as `INTENT_KEYWORDS`.

## Patient Type Keywords
Defined in `app/rules.py` as `PATIENT_TYPE_KEYWORDS`.

## Why This Matters
You can tweak rule sets without touching Router logic, making it easier to learn how routing affects agent collaboration.
