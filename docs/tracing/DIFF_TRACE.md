# State Diff Trace

## What It Shows
After each agent runs, a `state_diff` event lists the fields changed.

## Where to See
- In `outputs/trace.json` under events
- In `outputs/trace.html` highlighted as "diff"
