
## 用户身份认证和注册流程详解

### 一、核心组件

#### 1. 数据库层（3 种实现）

| 实现 | 文件 | 特点 |
|------|------|------|
| **内存版** | [db.py](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/tools/db.py) | `PatientDBTool` - 用于测试 |
| **SQLite 版** | [db_sqlite.py](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/tools/db_sqlite.py) | `SQLitePatientDB` - 轻量级存储 |
| **SQLAlchemy 版** | [db_sqlalchemy.py](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/tools/db_sqlalchemy.py) | `SQLAlchemyPatientDB` - 支持多种数据库 |

#### 2. 验证工具

[VerificationTool](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/tools/verification.py) - 封装验证逻辑：

```python
class VerificationTool:
    def __init__(self, db: PatientDBTool) -> None:
        self.db = db

    def verify(self, phone: str | None, dob: str | None) -> str | None:
        if not phone or not dob:
            return None
        return self.db.find_patient(phone, dob)  # 返回 patient_id
```

---

### 二、注册流程（Registration）

#### 流程图

```
用户："I want to register"
         │
         ▼
┌─────────────────────────────────────────┐
│  ① RouterAgent 识别意图                  │
│     intent = "registration"             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  ② SupervisorAgent 决策                  │
│     decide() → "proceed"                │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  ③ TaskAgent._handle_registration_...   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ Step 1: 检查必填字段             │   │
│  │ - first_name                    │   │
│  │ - last_name                     │   │
│  │ - dob (date of birth)           │   │
│  └──────────────┬──────────────────┘   │
│                 │                       │
│          字段完整？                     │
│         ┌──────┴──────┐                │
│         │             │                │
│        No            Yes               │
│         │             │                │
│         ▼             │                │
│  返回："Please tell   │                │
│  me your {missing}"   │                │
│                       ▼                │
│              ┌──────────────────────┐  │
│              │ Step 2: 创建患者记录  │  │
│              │ db.create_patient()  │  │
│              │                      │  │
│              │ patient_id =         │  │
│              │ "P{hash}%100000:05d" │  │
│              └──────────┬───────────┘  │
│                         │               │
│                         ▼               │
│              ┌──────────────────────┐  │
│              │ Step 3: 初始化报告    │  │
│              │ reports.seed_report()│  │
│              │ (预设医疗报告)        │  │
│              └──────────┬───────────┘  │
│                         │               │
│                         ▼               │
│              ┌──────────────────────┐  │
│              │ Step 4: 预约流程      │  │
│              │ calendar.get_slots() │  │
│              └──────────────────────┘  │
└─────────────────────────────────────────┘
```

#### 代码实现

[task.py:L60-95](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/agents/task.py#L60-L95)

```python
def _handle_registration_or_schedule(self, state: SessionState) -> str:
    fields = state.collected_fields
    
    # Step 1: 检查必填字段
    missing = []
    if not fields.first_name:
        missing.append("first name")
    if not fields.last_name:
        missing.append("last name")
    if not fields.dob:
        missing.append("date of birth")
    
    if missing:
        return f"Please tell me your {', '.join(missing)}."
    
    # Step 2: 创建患者记录
    if not state.patient_id:
        try:
            if isinstance(self.db, (SQLitePatientDB, SQLAlchemyPatientDB)):
                state.patient_id = self.db.create_patient(
                    fields, 
                    state.caller_phone  # 电话号码
                )
                
                # Step 3: 初始化医疗报告
                if isinstance(self.reports, (SQLiteReportStore, SQLAlchemyReportStore)):
                    self.reports.seed_report(
                        state.patient_id,
                        "Impression: Mild degenerative changes in L4-L5. No acute findings.",
                    )
        except Exception as exc:
            send_alert("db_create_patient_failed")
            append_error(state, "tool", "DB_ERROR", str(exc))
            return "I had trouble saving your information. Let me connect you to a person."
    
    # Step 4: 继续预约流程...
```

#### 字段提取

[task.py:L214-236](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/agents/task.py#L214-L236)

```python
def _extract_fields(self, state: SessionState, text: str) -> None:
    t = text.strip()
    fields = state.collected_fields
    
    # 提取名字
    match = re.search(r"first name is\s+(\w+)", t, re.IGNORECASE)
    if match and not fields.first_name:
        fields.first_name = match.group(1)
    
    # 提取姓氏
    match = re.search(r"last name is\s+(\w+)", t, re.IGNORECASE)
    if match and not fields.last_name:
        fields.last_name = match.group(1)
    
    # 提取完整姓名
    match = re.search(r"name is\s+(\w+)\s+(\w+)", t, re.IGNORECASE)
    if match:
        if not fields.first_name:
            fields.first_name = match.group(1)
        if not fields.last_name:
            fields.last_name = match.group(2)
    
    # 提取出生日期 (YYYY-MM-DD 格式)
    match = re.search(r"(\d{4}-\d{2}-\d{2})", t)
    if match and not fields.dob:
        fields.dob = match.group(1)
```

---

### 三、身份认证流程（Verification）

#### 认证触发场景

1. **修改/取消预约** ([reschedule/cancel](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/agents/task.py#L149-L167))
2. **查看医疗报告** ([report](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/agents/task.py#L169-L195))

#### 认证流程图

```
用户："I want to cancel my appointment"
         │
         ▼
┌─────────────────────────────────────────┐
│  TaskAgent._handle_modify(state)        │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 调用 _ensure_verified(state)    │   │
│  └──────────────┬──────────────────┘   │
│                 │                       │
│                 ▼                       │
│  ┌─────────────────────────────────┐   │
│  │ Step 1: 检查是否已验证           │   │
│  │ if state.verification_status    │   │
│  │    == "verified": return True   │   │
│  └──────────────┬──────────────────┘   │
│                 │                       │
│          未验证？                       │
│                 │                       │
│                 ▼                       │
│  ┌─────────────────────────────────┐   │
│  │ Step 2: 检查尝试次数             │   │
│  │ if verification_attempts >=     │   │
│  │    max_verification_attempts    │   │
│  │    (默认 2 次)                     │   │
│  └──────────────┬──────────────────┘   │
│                 │                       │
│          未超限？                       │
│                 │                       │
│                 ▼                       │
│  ┌─────────────────────────────────┐   │
│  │ Step 3: 调用验证工具             │   │
│  │ patient_id = verifier.verify(  │   │
│  │     phone=state.caller_phone,  │   │
│  │     dob=state.collected_fields.│   │
│  │         dob)                   │   │
│  └──────────────┬──────────────────┘   │
│                 │                       │
│                 ▼                       │
│          验证成功？                     │
│         ┌──────┴──────┐                │
│         │             │                │
│        Yes           No                │
│         │             │                │
│         ▼             ▼                │
│  state.patient_id   state.             │
│  = patient_id       verification_      │
│  state.             status =           │
│  verification_      "failed"           │
│  status =                              │
│  "verified"                            │
│         │             │                │
│         └──────┬──────┘                │
│                │                       │
│                ▼                       │
│  verification_attempts++               │
│                                        │
│  返回 True/False                       │
└─────────────────────────────────────────┘
         │
         │ 验证失败
         ▼
  返回："Please provide your 
         date of birth so I can 
         verify your identity."
```

#### 代码实现

[task.py:L197-L211](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/agents/task.py#L197-L211)

```python
def _ensure_verified(self, state: SessionState) -> bool:
    # Step 1: 已验证直接返回
    if state.verification_status == "verified":
        return True
    
    # Step 2: 检查尝试次数
    if state.verification_attempts >= CONFIG.max_verification_attempts:
        state.handoff_reason = "verification_failed"
        return False  # 触发转人工
    
    # Step 3: 执行验证
    patient_id = self.verifier.verify(
        state.caller_phone, 
        state.collected_fields.dob
    )
    
    state.verification_attempts += 1
    
    if patient_id:
        state.patient_id = patient_id
        state.verification_status = "verified"
        return True
    
    state.verification_status = "failed"
    return False
```

---

### 四、数据库实现细节

#### 1. SQLite 实现

[db_sqlite.py](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/tools/db_sqlite.py)

```python
class SQLitePatientDB:
    def __init__(self, path: str = "outputs/patient.db") -> None:
        self.path = path
        Path("outputs").mkdir(parents=True, exist_ok=True)
        self.conn = sqlite3.connect(self.path)
        self._init_schema()
    
    def _init_schema(self) -> None:
        cur = self.conn.cursor()
        cur.execute("""
            CREATE TABLE IF NOT EXISTS patient (
                patient_id TEXT PRIMARY KEY,
                first_name TEXT,
                last_name TEXT,
                dob TEXT,
                phone TEXT,
                email TEXT
            )
        """)
    
    def create_patient(self, fields: CollectedFields, phone: str | None) -> str:
        # 生成 patient_id: P + hash 取模
        patient_id = f"P{abs(hash(fields.first_name + fields.last_name + fields.dob)) % 100000:05d}"
        
        # INSERT OR REPLACE (支持覆盖更新)
        cur = self.conn.cursor()
        cur.execute(
            "INSERT OR REPLACE INTO patient (patient_id, first_name, last_name, dob, phone, email) VALUES (?, ?, ?, ?, ?, ?)",
            (patient_id, fields.first_name, fields.last_name, fields.dob, phone, fields.email),
        )
        self.conn.commit()
        return patient_id
    
    def find_patient(self, phone: str | None, dob: str | None) -> str | None:
        if not phone or not dob:
            return None
        
        cur = self.conn.cursor()
        cur.execute(
            "SELECT patient_id FROM patient WHERE phone = ? AND dob = ?", 
            (phone, dob)
        )
        row = cur.fetchone()
        return row[0] if row else None
```

#### 2. SQLAlchemy 实现

[db_sqlalchemy.py](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/tools/db_sqlalchemy.py)

```python
class Patient(Base):
    __tablename__ = "patient"
    patient_id = Column(String, primary_key=True)
    first_name = Column(String)
    last_name = Column(String)
    dob = Column(String)
    phone = Column(String)
    email = Column(String)

class SQLAlchemyPatientDB:
    def __init__(self) -> None:
        url = os.getenv("DATABASE_URL", "sqlite:///outputs/patient_sa.db")
        self.engine = create_engine(url)
        Base.metadata.create_all(self.engine)
        self.Session = sessionmaker(bind=self.engine)
    
    def create_patient(self, fields: CollectedFields, phone: str | None) -> str:
        patient_id = f"P{abs(hash(fields.first_name + fields.last_name + fields.dob)) % 100000:05d}"
        
        with self.Session() as session:
            # 使用 merge 实现 upsert
            session.merge(
                Patient(
                    patient_id=patient_id,
                    first_name=fields.first_name,
                    last_name=fields.last_name,
                    dob=fields.dob,
                    phone=phone,
                    email=fields.email,
                )
            )
            session.commit()
        return patient_id
    
    def find_patient(self, phone: str | None, dob: str | None) -> str | None:
        if not phone or not dob:
            return None
        
        with self.Session() as session:
            row = session.query(Patient).filter_by(phone=phone, dob=dob).first()
            return row.patient_id if row else None
```

---

### 五、状态管理

[SessionState](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/graph/state.py#L44-L61) 中的认证相关字段：

```python
@dataclass
class SessionState:
    session_id: str
    caller_phone: Optional[str] = None          # 来电号码
    patient_id: Optional[str] = None            # 患者 ID (注册后/验证后设置)
    verification_status: str = "unverified"     # unverified/verified/failed
    verification_attempts: int = 0              # 验证尝试次数
    collected_fields: CollectedFields = field(  # 收集的个人信息
        default_factory=CollectedFields
    )
```

[CollectedFields](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/graph/state.py#L33-L41):

```python
@dataclass
class CollectedFields:
    first_name: Optional[str] = None
    last_name: Optional[str] = None
    dob: Optional[str] = None            # 出生日期 (YYYY-MM-DD)
    email: Optional[str] = None
    insurance_provider: Optional[str] = None
    insurance_member_id: Optional[str] = None
    reason_for_visit: Optional[str] = None
```

---

### 六、完整交互示例

#### 示例 1: 新用户注册

```
用户："I want to schedule an appointment"
Agent: "Please tell me your first name, last name, and date of birth."

用户："My name is John Smith and my DOB is 1990-05-15"
  → _extract_fields() 提取：
    - first_name = "John"
    - last_name = "Smith"
    - dob = "1990-05-15"

Agent: (内部执行 db.create_patient())
  → patient_id = "P12345"
  → reports.seed_report("P12345", ...)
  → calendar.get_available_slots()
Agent: "I have 10:00 AM or 2:00 PM available. Which works for you?"

用户："10 AM works"
Agent: "You're all set. I have confirmed your appointment 
        and sent a text message."
```

#### 示例 2: 老用户取消预约

```
用户："I want to cancel my appointment"
  → RouterAgent: intent = "cancel"
  → TaskAgent._handle_modify()

Agent: "Please provide your date of birth so I can verify your identity."
  → _ensure_verified() 触发验证

用户："My DOB is 1990-05-15"
  → _extract_fields() 提取 dob
  → verifier.verify(phone="123-456-7890", dob="1990-05-15")
  → db.find_patient() 返回 "P12345"
  → state.patient_id = "P12345"
  → state.verification_status = "verified"

Agent: "Please provide your appointment ID so I can update it."

用户："My appointment ID is A001"
Agent: "Your appointment has been cancelled."
```

---

### 七、配置项

[config.py](file:///Users/jinniu/Documents/GitHub/ClinicVoxAI/app/config.py):

```python
@dataclass
class AppConfig:
    max_verification_attempts: int = 2  # 最大验证尝试次数
    storage_backend: str = "sqlite"     # sqlite/sqlalchemy/memory
```

---

### 八、安全特性

1. **双因素验证**：phone + dob 组合验证
2. **尝试次数限制**：超过 2 次失败转人工
3. **状态隔离**：verification_status 跟踪验证状态
4. **错误追踪**：errors 列表记录所有错误
5. **审计日志**：EventRecorder 记录所有状态变更

---

### 总结

**注册流程**：
1. 收集必填字段（first_name, last_name, dob）
2. 生成 patient_id 并保存到数据库
3. 初始化医疗报告
4. 继续预约流程

**认证流程**：
1. 检查是否已验证
2. 检查验证尝试次数
3. 使用 phone + dob 查询数据库
4. 验证成功设置 patient_id 和 verification_status
5. 失败超过 2 次转人工